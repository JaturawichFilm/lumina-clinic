# 🚀 Lumina Clinic - Project Summary & Interview Guide
คู่มือสรุปรายละเอียดโปรเจกต์ ระบบจองคิวผ่าน LINE OA (Decoupled Architecture)
เอกสารนี้รวบรวมข้อมูลและแนวคิดทั้งหมดของการออกแบบระบบ เพื่อใช้ทบทวนสำหรับการสัมภาษณ์งานและนำเสนอผลงาน (Portfolio)

---

## 🏗️ 1. ภาพรวมโปรเจกต์ (Project Overview)
**โจทย์ของโปรเจกต์:** สร้างระบบการจัดการคลินิกที่ลดภาระของพนักงานที่ต้องตอบแชท และช่วยให้ลูกค้าสามารถกดปุ่มจองคิวด้วยตัวเองผ่านแอปพลิเคชัน LINE ได้เลย โดยไม่ต้องสลับหน้าจอไปแอปอื่น (Frictionless Booking)

**การแก้ปัญหา (Solution):** พัฒนาระบบ Automated Booking System ที่ฝังตัวเข้าไปใน LINE Official Account (ผ่านเทคโนโลยี LIFF) และมีระบบดูแลลูกค้า (Admin Dashboard) สำหรับให้พนักงานคลินิกอนุมัติคิวและดูประวัติ

---

## 🎨 2. สถาปัตยกรรมระบบ (Decoupled Microservices Architecture)
เราตั้งใจออกแบบแอปพลิเคชันโดยแยกส่วนประกอบของระบบออกจากกันเป็น **3 ส่วนแยกอิสระ (Decoupled)** แทนที่จะจับมัดรวมกัน (Monolith) ซึ่งเป็น Best Practice ของระบบที่รับจำนวนคนใช้งานเยอะๆ มีข้อดีหลักๆ ดังนี้:

1. 🔒 **ความปลอดภัย (Security):** โค้ดที่ลูกค้าเห็นในมือถือ (Frontend) จะถูกแยกขาดจากโค้ดของหลังบ้าน (Admin) ทำให้แฮกเกอร์ไม่มีทางเจาะดูหน้าแอดมินหรือดูข้อมูลฐานข้อมูลลูกค้าของคนอื่นผ่านทางมือถือได้เลย
2. 🚀 **โหลดไวและคล่องตัว (Performance):** การแยก Frontend ทำให้ไฟล์เว็บไซต์ของลูกค้ามีขนาดเล็กจิ๋ว ข้อมูลโหลดเข้าหน้าจอจากแอป LINE ภายในเสี้ยววินาที โดยไม่ต้องรอโหลดตารางหรือกราฟหนักๆ ที่มีเฉพาะของแอดมิน
3. 🛠️ **การบำรุงรักษา (Maintenance):** หากระบบจองคิวหน้าบ้านพัง หรือกำลังอัปเดตเวอร์ชัน แอดมินก็ยังล็อกอินเข้าไปใช้ระบบได้ปกติ ระบบจะไม่ล่มพร้อมกัน
4. 📱 **คนละแพลตฟอร์มชัดเจน:** Frontend ออกแบบตัว UI แบบ Mobile-First สำหรับหน้าจอโทรศัพท์ (LIFF) ส่วน Admin Dashboard ออกแบบ UI แบบ Desktop-First สำหรับจอกว้าง (iPad/Computer)

---

## 💻 3. เทคโนโลยีที่เลือกใช้ (Tech Stack & Justification)

*   **Frontend (LIFF) & Admin Dashboard:** `React 18` + `Vite`
    *   *เหตุผลที่ใช้:* React จัดการระบบแบบ Component ทำให้ใช้ปุ่มกดเดิมซ้ำๆ ได้ และ Vite ช่วยให้ระบบ Build ตอนขึ้นเซิร์ฟเวอร์เร็วกว่าเครื่องมือยุคเก่า
*   **Backend (API Server):** `Node.js` + `Express`
    *   *เหตุผลที่ใช้:* เขียนด้วย JavaScript ทั้งหมด ทำให้ทั้ง Frontend/Backend ใช้ภาษาเดียวกัน (ลดระยะเวลาเรียนรู้ภาษาใหม่) และสามารถเชื่อมต่อ Webhook ของ LINE API ได้เสถียร
*   **Database:** `PostgreSQL` รันบนคลาวด์ของ `Supabase`
    *   *เหตุผลที่ใช้:* ต้องการโครงสร้างฐานข้อมูลที่มีความสัมพันธ์แบบเชื่อมโยงกันได้ (Relational SQL) เพื่อเชื่อมโปรไฟล์ผู้ใช้กับการจองหลายๆ รายการได้
*   **ORM (Database Router):** `Prisma ORM`
    *   *เหตุผลที่ใช้:* ป้องกันความผิดพลาดจากการพิมพ์คำสั่ง SQL ผิด (ประกอบด้วย Type-Safety) Prisma จะช่วยโยง Foreign Key และอัปเดตโครงสร้าง (Migration) ให้อัตโนมัติ ช่วยลดเวลาการเขียนโค้ดหลังบ้านอย่างมาก
*   **Deployment:** `Vercel (Serverless)`
    *   *เหตุผลที่ใช้:* รองรับระบบ CI/CD ดึงโค้ดจาก GitHub ไปรันแบบ Serverless อัตโนมัติ (สามารถรับ Traffic กะทันหัน หรือสเกลขยายเซิร์ฟเวอร์ให้เองถ้าคนเข้าเยอะ) และรองรับโปรเจกต์หลายๆ ตัวพร้อมกันในฟรีเทียร์

---

## 🗄️ 4. โครงสร้างฐานข้อมูล (Database Schema)
อธิบายตารางที่มีใน Supabase ตามที่นิยามคู่กับ Prisma แบบ **One-to-Many Relationship** (ลูกค้า 1 คน สามารถเข้ามารับบริการจองคิวได้หลายครั้ง)

### 👤 4.1 ตาราง `Lead` (ตารางเก็บโปรไฟล์ลูกค้า)
เก็บข้อมูลตัวตนตั้งต้น เพื่อเป็นประวัติไว้ให้แอดมินดู
*   `id` : (Primary Key) รหัสลูกค้า แรนดอมด้วยแพทเทิร์น Cuid ไม่ให้ซ้ำกัน
*   `lineId` : รหัส UID พิเศษของแพลตฟอร์ม LINE (ตั้งเป็น Unique) นำไปใช้ดึงข้อมูลลูกค้าในภายหลังและเอาไปใช้ยิงข้อความส่วนตัว (Push Message) กลับไปหาถูกคน
*   `displayName` : ชื่อโปรไฟล์ LINE ของลูกค้า
*   `pictureUrl` : ลิงก์รูปโปรไฟล์ (ดึงมาจากระบบของ LINE อัตโนมัติ)
*   `status` : ฟิลด์คัดกรอง แยกสถานะว่า เป็นลูกค้าทักมาครั้งแรก (NEW) หรือ โทรคุยติดตามผลแล้ว (CONTACTED)

### 📅 4.2 ตาราง `Appointment` (ตารางเก็บข้อมูลจองคิว)
สร้างมาเพื่อคิวบริการโดยเฉพาะ แยกตัวออกมาไม่ให้ไปรกในตารางลูกค้า
*   `id` : (Primary Key) รหัสประจำคิวจอง
*   `leadId` : **(Foreign Key)** กุญแจผูกความสัมพันธ์ชี้กลับไปที่ตาราง `Lead` เพื่อบอกให้รู้ว่าบิลคิวจองใบนี้เป็นของลูกค้าคนไหน
*   `service` : งานบริการที่ลูกค้าร้องขอ เช่น ปรึกษา, เลเซอร์ 
*   `date` / `time` : วันเวลานัดหมาย
*   `phone` : เบอร์โทรติดต่อ
*   **`status` (ฟิลด์หัวใจสำคัญ) :** ค่าเริ่มต้นคือ PENDING 
    *ความว้าว:* ระบบ Backend ได้เขียนดักจับไว้ หากแอดมินแก้ไข Status เป็น `CONFIRMED` ระบบจะสับสวิตช์ สั่งยิง LINE Message (Push API) วิ่งไปแจ้งเตือนลูกค้าว่า "ยืนยันคิวแล้วนะ!" โดยอัตโนมัติ

---

## 🚀 5. สรุปขั้นตอนการเปลี่ยนจาก Development ไปหา Production
1. **แก้ลิงก์ API ฝั่ง Frontend:** เปลี่ยนจากลิงก์ `localhost:` (เครื่องส่วนตัว) และ `Ngrok` เป็น URL จริงบนคลาวด์ Vercel
2. **แปลงโค้ดระบบ Backend อัตโนมัติเป็น Serverless:** ไม่ได้ใช้ `app.listen()` ค้างไว้เฉยๆ แต่เพิ่มสคริปต์ `vercel.json` ปรับใช้เซิร์ฟเวอร์แบบติดสปีด 
3. **กำหนด Environment Variables ซ่อนรหัสผ่าน:** ติดตั้งค่า DATABASE_URL และ LINE_TOKEN ไว้บนแดชบอร์ด Vercel ทำให้ไม่ต้องส่งรหัสผ่านส่วนตัวขึ้นไปบน GitHub เพื่อความปลอดภัย
4. **อัปเดต LINE Webhook:** โยง URL เว็บตัวใหม่กลับไปตั้งค่าใน LINE Developer (Webhook เพื่อให้แชทบอทตอบกลับ) และ LIFF Endpoint URL

---
**Prepared by:** Lumina Clinic Development Project 
**Role:** Full-Stack Developer 
